{% extends 'dashboard/index.html.twig' %}

{% block title %}Détail catégorie{% endblock %}

{% block body %}


<div class="container">
        <h2 class="text-center mt-5 mb-3">Détail catégorie</h2>
        <div class="card">
            <div class="card-header">
                <a class="btn btn-outline-info float-right" href="{{ path('app_categorie_list') }}"> 
                    Liste des catégories
                </a>
            </div>
            <div class="card-body">
           <p> Nom: {{ detailcategorie.nom }}</p>
            </div>
        </div>
    </div>
{% endblock %}