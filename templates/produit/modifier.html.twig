{% extends 'dashboard/index.html.twig' %}

{% block title %}Modifier produit{% endblock %}

{% block body %}
 
 <div class="container">
        <h2 class="text-center mt-5 mb-3">Modifier produit</h2>
        <div class="card">
            <div class="card-header">
                <a class="btn btn-outline-info float-right" href="{{ path('app_produit_list') }}"> 
                   Liste des produits
                </a>
            </div>
            <div class="card-body">
                 {{ include('produit/form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}